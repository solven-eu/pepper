<assembly
	xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2 
  http://maven.apache.org/xsd/assembly-1.1.2.xsd">
	<!-- https://maven.apache.org/guides/mini/guide-assemblies.html -->

	<id>spark</id>
	<formats>
		<!-- We need all jars to be available in a single folder -->
		<format>dir</format>
	</formats>
	<includeBaseDirectory>false</includeBaseDirectory>
	<baseDirectory>jars</baseDirectory>
	<dependencySets>
		<dependencySet>
			<useProjectArtifact>false</useProjectArtifact>
			<outputDirectory>/jars</outputDirectory>
			<scope>compile</scope>
			<excludes>
				<exclude>org.apache.spark:*</exclude>
			</excludes>
			<outputFileNameMapping>${artifact.groupId}-${artifact.artifactId}-${artifact.version}${dashClassifier?}.${artifact.extension}</outputFileNameMapping>
		</dependencySet>
		<dependencySet>
			<useProjectArtifact>false</useProjectArtifact>
			<outputDirectory>/jars</outputDirectory>
			<scope>compile</scope>
			<includes>
				<include>org.apache.spark:*</include>
			</includes>
			<!-- docker-image-tool expects Spark jars not to be named with groupId prefix -->
			<outputFileNameMapping>${artifact.artifactId}-${artifact.version}${dashClassifier?}.${artifact.extension}</outputFileNameMapping>
		</dependencySet>
	</dependencySets>
	<fileSets>
		<fileSet>
			<directory>src/main/resources/spark-3.1.2</directory>
			<outputDirectory>/</outputDirectory>
		</fileSet>
	</fileSets>
</assembly>